import React from 'react'
import ProjectCard from './ProjectCard'

import projects from '../data/projects.json'

export default function ProjectCards() {
  const school = projects[0].school

  console.log(school)
  return <div></div>
}
